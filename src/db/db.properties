# Database Configuration
# For Dhwani's Portfolio Database

# MySQL Database Connection
db.url=jdbc:mysql://db:3306/portfolio_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.user=root
db.password=rootpass

# Optional: Connection Pool Settings
db.max.pool.size=20
db.min.pool.size=5
db.connection.timeout=30000

# Note: Update db.user and db.password according to your MySQL configuration
